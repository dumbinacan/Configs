#!/bin/sh

# I think I want to call a bunch of scripts to set up everything
# moving to flatpak, so installing the base shit I want on flat pak should go here
# https://aur.archlinux.org/cgit/aur.git/snapshot/yay.tar.gz
# flatpak override --env=SIGNAL_PASSWORD_STORE=kwallet org.signal.Signal
# moving back to the nextcloud gui
# maybe some sort of reminder of the directories for nextcloud idk
# this script should set up all the configs
# install yay
# install all the flatpaks
mkdir -p ~/Notes ~/Projects
