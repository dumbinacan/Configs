# aliases.zsh

alias sudo='sudo '
alias dd='dd status=progress'
alias ping='ping -c3'
alias journal='~/Configs/init/zsh/journal.sh'
alias cmatrix='neo-matrix'

# Temporary to know why it might fail while setting up my network
alias ssh="echo '/etc/clamav/clamd.conf has AlertPhishingSSLMismatch enabled and may cause false positives' && ssh"
# SSH shortcuts
alias storagebin='ssh marco@storageb.in'
